---
const { item } = Astro.props;
const cardClass =
  "group block overflow-hidden rounded-lg border border-(--color-surface-elevated) bg-(--color-surface) transition-all duration-400 hover:border-(--color-accent-dim) hover:shadow-[0_20px_40px_-12px_rgba(0,0,0,0.5)]";
---

{item.link ? (
  <a href={item.link} target="_blank" rel="noopener noreferrer" class={cardClass}>
    <div class="relative flex h-52 w-full items-center justify-center overflow-hidden bg-(--color-surface-elevated)">
      <img
        class="max-h-full max-w-full object-contain transition-transform duration-500 group-hover:scale-105"
        src={item.image}
        alt={item.alts}
      />
      <div class="absolute inset-0 bg-linear-to-t from-(--color-surface) via-transparent to-transparent opacity-50 pointer-events-none" />
    </div>
    <div class="p-6">
      <h3 class="font-display text-xl font-semibold tracking-tight text-(--color-text) group-hover:text-(--color-accent) transition-colors">
        {item.title}
      </h3>
      <p class="mt-2 font-body text-(--color-text-muted) leading-relaxed">
        {item.description}
      </p>
      <span class="mt-4 inline-flex items-center gap-2 font-display text-sm font-medium text-(--color-accent)">
        Visit
        <svg class="w-4 h-4 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </span>
    </div>
  </a>
) : (
  <div class={cardClass}>
    <div class="relative flex h-52 w-full items-center justify-center overflow-hidden bg-(--color-surface-elevated)">
      <img
        class="max-h-full max-w-full object-contain transition-transform duration-500 group-hover:scale-105"
        src={item.image}
        alt={item.alts}
      />
      <div class="absolute inset-0 bg-linear-to-t from-(--color-surface) via-transparent to-transparent opacity-50 pointer-events-none" />
    </div>
    <div class="p-6">
      <h3 class="font-display text-xl font-semibold tracking-tight text-(--color-text) group-hover:text-(--color-accent) transition-colors">
        {item.title}
      </h3>
      <p class="mt-2 font-body text-(--color-text-muted) leading-relaxed">
        {item.description}
      </p>
    </div>
  </div>
)}

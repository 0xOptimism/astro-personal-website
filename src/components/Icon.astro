---
const { item, size } = Astro.props;
---

<i
  style={{ color: item.color }}
  class={`${size === 'lg' ? 'text-2xl' : 'text-md'} devicon-${item.icon}-${
    item.type ? item.type : 'plain'
  }`}></i>
